git clone 

//

git config --global user.email "you@example.com"
git config --global user.email "Your name"

// editing ..
// finish editing ..

git status // checking to which is not added

git add "filename or dir name" // add is finished 

git commit // write commit msg

git push origin master // upload your code


//

git pull origin master // download our code
